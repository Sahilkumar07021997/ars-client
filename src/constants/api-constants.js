/**
 * SubSystem Constants
 * 
 * Defines constants for various subsystems within the application, 
 * representing different functionalities and modules.
 * 
 * @returns {Object}
 */
export const SubSystem = {
	AUTH: "auth",
	USER: "user",
	REPORTS: 'rpt',
	ADMIN: 'a',
	COMMON: 'c',
	// add new sub system as required.
};


/**
 * ApiResponseType
 * 
 * Contains constants for API response types.
 * 
 * @returns {Object}
 */
export const ApiResponseType = {
	JSON: 1,
	BUFFER: 2
}